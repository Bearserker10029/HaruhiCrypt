[package]
name = "haruhi-crypt"
version = "0.1.0"
edition = "2021"
description = "HaruhiCrypt - Encryption using the Haruhi Problem (Superpermutations)"
license = "MIT"
repository = ""
keywords = ["cryptography", "encryption", "permutation", "haruhi"]
categories = ["cryptography"]

[dependencies]
sha2 = "0.10"
hmac = "0.12"
rand = "0.8"
digest = "0.10"
rfd = "0.14"
chrono = "0.4"
image = { version = "0.24", default-features = false, features = ["jpeg"] }
winapi = { version = "0.3", features = ["winuser", "windef", "wingdi", "combaseapi", "objbase", "shobjidl", "uxtheme", "minwindef"] }

[dependencies.egui]
version = "0.25"

[dependencies.eframe]
version = "0.25"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true